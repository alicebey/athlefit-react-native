const {StyleSheet} = require('react-native');

// Styles for all change password screen
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#212529',
    paddingHorizontal: 20,
    paddingVertical: 25,
    justifyContent: 'space-between',
  },
});

export default styles;
